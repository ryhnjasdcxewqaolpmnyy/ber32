apiVersion: v1
kind: Service
metadata:
  name: poile
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "poile"
    port: 8080
  selector:
    app: poile
