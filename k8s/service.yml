apiVersion: v1
kind: Service
metadata:
  name: swet
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "swet"
    port: 8080
  selector:
    app: swet
