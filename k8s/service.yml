apiVersion: v1
kind: Service
metadata:
  name: trecd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "trecd"
    port: 8080
  selector:
    app: trecd
