apiVersion: v1
kind: Service
metadata:
  name: faret
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "faret"
    port: 8080
  selector:
    app: faret
