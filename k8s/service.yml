apiVersion: v1
kind: Service
metadata:
  name: dcot
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dcot"
    port: 8080
  selector:
    app: dcot
